version: '3.8'
services:
  familist:
    build:
      context: .
      args:
        - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "3000:3000"
    volumes:
      - ./database.json:/app/database.json
    restart: always
